
import 'package:flutter/material.dart';

class editMachine extends StatelessWidget {
  const editMachine({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
